import React from 'react';
import { Text } from 'react-native';
import { theme, css } from './theme';

const styles = css({
    button: {
        backgroundColor: theme.error_color,
        fontSize: theme.font_xs,
        color: 'white',
        borderRadius: '50%',
        padding: 10
    }
});

export const Badge = ({ style, ...props }) => <Text style={[styles.button, style]} {...props} />;
